#include "main.h"
/**
 * main - function that checks for lowercase character.
 *
 * Function: always 0 
 */
int _islower(int c)
{
	int ch = 'c';

	if (islower(ch))
	{
	_putchar(1);
	}
	else
	{
	_putchar(0);
	}
	_putchar('\n');

	return (0);
}
