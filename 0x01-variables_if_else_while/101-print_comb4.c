#include <stdio.h>                                         
#include <stdlib.h>                                        
#include <limits.h> 
/**
 * main - program that prints all possible different combinations of three digits.
 * 
 * Return: always 0 (success)
 */
int main(void)
{
	int n, x, y:
{
	for (n =0; n <= 999; n++)
{	
	for (x = n + 2; x <= 999; x++)
{	
	for (y = x + 2; y <= 999;)
{
	putchar(n

