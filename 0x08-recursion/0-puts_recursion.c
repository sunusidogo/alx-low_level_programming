#include "main.h"

/**
 * _puts_recursion - function to write a string.
 * @s: the character to write
 *
 * Return: 0
 */
void _puts_recursion(char *s)

{
	int i;
	*s = &i;	

	if (i = 'a'; i < 'z')
	       i++;
	_putchar(s);
	_putchar('\n');
	return (0);

}
