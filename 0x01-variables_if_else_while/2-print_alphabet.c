#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the alphabets in lowercase, and then UPPERCASE
 *
 * Return: always 0
 */
void main(void)
{
	char chl = 'a';
	char chu = 'A';

	for (ch <= 'z';)
		putchar("%c\n", chl++);
	for (ch <= 'Z';)
		putchar("%c\n", chu++);

	return (0);
}
