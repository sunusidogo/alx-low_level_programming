#include <stdio.h>
/**
 * main - print all the single digit numbers in base 10
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int n;
{
	for (n = 0; n < 10; n++)

	putchar(n);
	putchar('\n');
}
	return (0);
}
