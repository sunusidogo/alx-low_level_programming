#include "main.h"

/**
 * _isdigit - checks for a digit from 0 to 9
 * @c: integer c
 *
 * Return: 0
*/
int _isdigit(int c)
{

	if (c >= 0 || c <= 9)		
	_putchar(1);

	else
	_putchar(0);
	_putchar('\n');

	return (0);
}
