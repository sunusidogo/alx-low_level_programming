#include "main.h"
#include <ctype.h>
/**
 * main - function that checks for alphabetic character
 *
 * Return: always 0 (success)
 */
int _isalpha(int c)
{
	int ch = 'c';

	if (isascii && islower || isupper(ch))
	{
		_putchar(1);
	}
	else
	{
		_putchar(1);
	}
}

