#include "main.h"

/**
 * mul - compute multiplication of two intergers a and b.
 * @a: a is an int to be multiply by
 * @b: b is a multiplier
 *
 * Return: 0
 *
 */
int mul(int a, int b)
{

	int mul;

	mul = a * b;
	return (mul);
}
