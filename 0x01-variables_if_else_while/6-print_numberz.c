#include <stdio.h>
/**
 * main - print all the single digit numbers in base 10
 *
 * Return: always 0 (success)
 */
int main(void)
{
	int num;
{
	for (num = 0; num <= 9; num++)
{
	putchar(num);
	putchar('\n');
}
}
	return (0);
}
